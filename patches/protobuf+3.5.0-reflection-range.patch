diff -ruN a/src/reflect/dynamic/mod.rs b/src/reflect/dynamic/mod.rs
--- a/src/reflect/dynamic/mod.rs	2025-07-21 16:11:49
+++ b/src/reflect/dynamic/mod.rs	2025-07-21 16:11:08
@@ -341,6 +341,8 @@
     }
 
     fn merge_from_dyn(&mut self, is: &mut CodedInputStream) -> crate::Result<()> {
+        let start_pos = is.pos();
+        
         while !is.eof()? {
             let (field, wire_type) = is.read_tag_unpack()?;
             let field_desc = match self.descriptor.field_by_number(field) {
@@ -387,6 +389,10 @@
                 }
             }
         }
+        
+        let end_pos = is.pos();
+        self.special_fields.set_range((start_pos, end_pos));
+        
         Ok(())
     }
 
